---
# defaults file for ansible-role-fleet-key-pairs
ssh_user: "{{ ansible_env.USER }}"
ssh_file_path: "{{ansible_env.HOME}}/.ssh"

p3t_input: {}
  # - { ssh_key: "root_local.example.com", ssh_key_type: "rsa", ssh_key_bits: 4096, ssh_key_path: "example", ssh_passphrase: "", state: present }
