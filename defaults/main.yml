---
# defaults file for ansible-role-fleet-key-pairs
ssh_user: "{{ ansible_env.USER }}"
ssh_key_path: "{{ansible_env.HOME}}/.ssh"

p3r_input: {}
  # - { ssh_key: "root_local.example.com", ssh_key_type: "rsa", ssh_key_bits: 4096, environment: "example", ssh_passphrase: "", state: present }
